import { ExerciseRemovedPipe } from './exercise-removed.pipe';

describe('ExerciseRemovedPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseRemovedPipe();
    expect(pipe).toBeTruthy();
  });
});
